<h1>Cart</h1>

<ol>
  {% for item in cart.items %}
    <li>{{ item.product.title }} - {{ item.quantity }} - {{ item.final_price | money }}</li>
  {% endfor %}
</ol>
<div>
  <strong>Total:</strong>
  {{ cart.total_price | money }}</div>