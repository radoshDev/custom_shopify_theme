<!DOCTYPE html>
<html lang="en">
  <head>
    {{ content_for_header }}
    {{ "base.css" | asset_url | stylesheet_tag }}
    <script src="{{ 'main.js' | asset_url }}" defer="defer"></script>
  </head>
  <body>
    {% render 'header' %}
    {{ content_for_layout }}
  </body>
</html>